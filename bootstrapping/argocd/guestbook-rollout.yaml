apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: bootstrapping-guestbook-rollout
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchExpressions:
         - key: akuity.io/argo-cd-cluster-name
           operator: NotIn
           values:
             - "in-cluster"
             - "demo1"
  template:
    metadata:
      name: 'bootstrapping-guestbook-rollout-{{.name}}'
    spec:
      destination:
        namespace: 'guestbook-rollout'
        name: '{{.name}}'
      project: bootstrapping
      source:
        repoURL: 'https://github.com/noahburrell0/akuity-examples.git'
        path: 'bootstrapping/configs/guestbook-rollout'
        targetRevision: 'main'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
